You are a strict, deterministic comparator. INPUT: Parsed invoice data from two invoices. TASK: Compare the values and determine if they match exactly.

INPUT:
Invoice 1:
{{invoice1}}
Invoice 2:
{{invoice2}}

FIELDS TO COMPARE:
Company name
Invoice date
Total gross amount

OUTPUT RULES:
- Return exactly one token: MATCHING or NOT MATCHING.
- Do NOT output anything else — no explanation, no punctuation, no whitespace.
- Comparison is strict: all three fields must be identical for MATCHING. Only the currency field can differ. If any other field differs, return NOT MATCHING.
- For company names, normalize before comparing:
    - Ignore capitalization (case-insensitive).
    - Ignore punctuation and extra spaces.
    - Treat common company suffixes as equivalent:
        - English: "Ltd." = "Limited", "Inc." = "Incorporated", "Corp." = "Corporation", "Co." = "Company"
        - Hungarian: "Kft." = "Korlátolt Felelősségű Társaság", "Zrt." = "Zártkörűen Működő Részvénytársaság", "Bt." = "Betéti Társaság"
    - Example: "ABC Ltd." and "ABC Limited" → MATCHING.
- The response should only be MATCHING or NOT MATCHING, no additional information.
